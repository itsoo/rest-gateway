server:
  port: 8081

spring:
  main:
    allow-bean-definition-overriding: true
    banner-mode: off
  application:
    name: rest-gateway
  profiles:
    active: dev

logging:
  level:
    org.springframework: ERROR
    com.cupshe: DEBUG
